NAME
    clear - clear the terminal screen

SYNOPSIS
    clear

DESCRIPTION
    The clear command clears the terminal screen and moves the cursor to the top-left corner.
    It is commonly used to refresh the display by removing previous output, providing a clean screen 
    for new output.
    This command is equivalent to the 'cls' command in Windows.

EXAMPLES
    - Clear the terminal screen:
        clear

SEE ALSO
    cls - clear terminal with cmd

NOTES
    The clear command usually only clears the visible screen 